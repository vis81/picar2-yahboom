# editors
.vscode
*.swp
*~

# python
.venv

# build
/build*
/twister-out*

__pycache__/

# West-managed directories (T1 topology — workspace root = repo root)
# Note: /zephyr/ is NOT listed here — it contains our own module.yml.
#       Zephyr OS is cloned into /deps/zephyr/ to avoid that conflict.
/deps/
/modules/
/bootloader/
/.west/

# SDKs and legacy isolated workspaces
/sdks/
/ws/
